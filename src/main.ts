import './style.scss'
import './projects.scss'

import Typed from 'typed.js';

document.addEventListener('DOMContentLoaded', () => {
  const typed = new Typed('#element', {
    strings: ['website under construction'],
    typeSpeed: 50,
  });
});
